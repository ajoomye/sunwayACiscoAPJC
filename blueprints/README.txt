To successfully run the scripts, go through the following steps first:

1. Run the following command in the git bash in the current "blueprints" file:

	export NET_TEXTFSM='./ntc-templates/ntc_templates/templates'

2. Install netmiko, flask in your cmd terminal:

	pip install netmiko
	pip install flask

3. Run app.py